export type NativeTimeout = ReturnType<typeof setTimeout>;

export interface Project {
    id: string;
    title: string;
    imageUrl: string;
    description: string;
    // Add more fields as needed for your project details
  }
  